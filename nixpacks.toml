[phases.setup]
nixPkgs = ["maven", "openjdk17"]

[phases.install]
cmds = ["cd backend && mvn clean install -DskipTests"]

[phases.build]
cmds = ["cd backend && mvn package -DskipTests"]

[start]
cmd = "cd backend && java -jar target/backend-0.0.1-SNAPSHOT.jar" 